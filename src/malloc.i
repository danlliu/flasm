;label:   opcode    operands, operands      ; comment
;
; malloc.i: heap allocation
; 
; methods:
;  - malloc (rdi = size_t)
;  - free (rdi = ptr)
;

          extern    malloc
          extern    free
